<!-- -->
<launch>

  <!-- send table.xml to param server -->
  <arg name="use_gnss" default="1" />
  <arg name="queue_size" default="10" />
  <arg name="downsampling_method" default="voxelgrid" />
  <arg name="sample_num" default="2000" />
  <arg name="sync" default="false" />
  
  <node pkg="ndt_localizer" type="ndt_matching" name="ndt_matching" output="screen">
    <param name="use_gnss" value="$(arg use_gnss)" />
    <param name="queue_size" value="$(arg queue_size)" />
    <param name="downsampling_method" value="$(arg downsampling_method)" />
    <param name="sample_num" value="$(arg sample_num)" />
    <remap from="/points_raw" to="/sync_drivers/points_raw" if="$(arg sync)" />
  </node>
  
</launch>
